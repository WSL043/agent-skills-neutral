# Implementation variants

## Theme selection

Choose or derive a compact token set from the audience, content, and medium. Avoid browsing large preset libraries when a direction is already clear.

## Color and typography mode

Focus on semantic color roles, font pairing, scale, line length, and fallback behavior.

## Style-system mode

Extend tokens into composition, shape, imagery, chart styling, and reusable artifact rules.
