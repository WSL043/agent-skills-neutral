interface:
  display_name: "Model Domain"
  short_description: "Neutral workflow: Model Domain"
  default_prompt: "Use $model-domain to create a domain model that makes rules and invalid states explicit before code structure hardens around ambiguity."
