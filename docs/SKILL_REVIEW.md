# Skill Implementation and Retention Review

All 27 entries are vendor-neutral thinking workflows. No provider-only, tool-manual, file-format, domain-adapter, deprecated, placeholder, organization-specific, or B-level skill remains active.

## Active skills

The always-on thinking core is separate from this on-demand catalog. S marks high-transfer workflows and A marks scenario workflows; neither level is a persistent default profile.

| Level | Skill | Concrete implementation | Risk or limitation | Routing decision |
|---|---|---|---|---|
| A | `instrument-observability` | Start from operational questions, add bounded-cardinality logs/metrics/traces and correlation, then verify emitted signals, redaction, dashboards, and actionable alerts. | Useful only with the real telemetry backend and representative traffic; avoid secrets and unbounded labels. | Stable on-demand thinking workflow. |
| A | `migrate-system-safely` | Inventory consumers and source of truth, run expand/backfill/switch/contract stages with reconciliation and rollback, and remove compatibility only after measured zero use. | Cleanup requires measured consumer and runtime evidence, not elapsed time or a successful deploy alone. | Stable on-demand thinking workflow. |
| A | `design-frontend` | Infer or confirm a brief, establish one coherent design system, implement hierarchy and complete states responsively, and audit accessibility, performance, and reference drift. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| A | `design-motion` | Use motion only for feedback, continuity, state, or hierarchy; specify timing/easing/interruption, then test reduced motion and performance. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| A | `design-visual-theme` | Translate product and brand intent into semantic color/type/spacing tokens, test representative content, and apply them through shared styles. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| A | `optimize-performance` | Measure a comparable baseline, isolate the bottleneck, make one targeted change, remeasure correctness and impact, and install a durable guard. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| A | `simplify-code` | Define the observable contract, simplify only proven incidental complexity, and verify behavior with focused and broader checks. | No material design defect found; still requires task-local current evidence and verification. | Stable but exact-trigger-only to prevent false-positive routing. |
| A | `evaluate-agent` | Define the claim and graders before running like-for-like baseline/candidate tasks, persist traces, and analyze failures without hiding uncertainty. | Intentional treatment-variable differences must be separated from uncontrolled confounds. | Stable on-demand thinking workflow. |
| A | `handoff-task-context` | Capture objective, verified current state, evidence, decisions, frontier, blockers, next actions, and bounded file references; reconcile every claim when resuming. | A handoff is a claim, not authority; resume mode must reconcile it with live repository and runtime state. | Stable on-demand thinking workflow. |
| A | `orchestrate-agent-work` | Map the decision frontier, keep the blocker local, delegate isolated tracer slices with explicit outputs, then integrate and run cross-task checks. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| A | `prototype-solution` | Time-box the smallest visual or executable artifact that can accept/reject one explicit uncertainty. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| A | `coauthor-documents` | Agree audience and outline first, draft claims with evidence section by section, then run a fresh-reader pass. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| A | `evaluate-scientific-evidence` | Separate reported results from interpretation, test whether design and measurement identify the claim, assess bias and statistical inference, then bound each conclusion to the evidence. | Appraisal frameworks are design- and domain-specific; reporting completeness, statistical significance, and evidence hierarchy are not standalone validity scores. | Stable but exact-trigger-only to prevent false-positive routing. |
| A | `formulate-scientific-hypotheses` | Freeze observations, state the claim type, generate rival explanations, derive discriminating predictions, and match measurement and analysis to the question. | No material design defect found; still requires task-local current evidence and verification. | Stable but exact-trigger-only to prevent false-positive routing. |
| A | `research-primary-sources` | Discover broadly, read authoritative primary sources, track versions and contradictions, and cite each material claim. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| A | `review-security-practices` | Trace real security data flows, compare current official guidance, validate reachable findings, and prioritize hardening with verification. | No material design defect found; still requires task-local current evidence and verification. | Stable but exact-trigger-only to prevent false-positive security routing. |
| A | `threat-model-system` | Map assets, flows, trust boundaries, attacker goals, attack paths, controls, owners, and residual risk. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| A | `design-codebase` | Map dependencies and change pressure, compare structural options, then stage an incremental migration with rollback points. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| A | `model-domain` | Build a glossary, entities/values, invariants, events, state transitions, ownership, and bounded contexts from real scenarios. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| A | `review-api-design` | Inventory consumers and compatibility promises, review resources/operations/schemas/errors/evolution, then report evidence, impact, recommendation, and severity. | API conventions are contextual and can change; repository contracts and current primary standards override generic preferences. | Stable on-demand thinking workflow. |
| S | `develop-with-tdd` | Run a strict red-green-refactor cycle around observable behavior, with characterization tests for legacy code. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| S | `diagnose-software` | Reproduce and minimize, instrument the first divergence, test one falsifiable hypothesis, fix the root cause, and add regression evidence. | Intermittent failures may require probabilistic or repeated evidence rather than deterministic reproduction. | Stable on-demand thinking workflow. |
| S | `review-code` | Review requirement fit and implementation quality separately, validate reachable findings, and support self/request/receive review modes. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| S | `verify-completion` | Map each claim to a fresh authoritative check, verify side effects and final state, and narrow claims when checks are blocked. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| S | `execute-plan` | Validate plan assumptions, execute dependency-ordered batches, run step evidence, and stop when a new decision changes scope. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| S | `plan-implementation` | Inspect the repository and produce dependency-ordered tracer slices with file/symbol steps, dependency edges, migration, rollback, and acceptance evidence. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
| S | `clarify-requirements` | Separate facts, assumptions, constraints, and open decisions; ask only high-information questions and produce testable acceptance criteria. | No material design defect found; still requires task-local current evidence and verification. | Stable on-demand thinking workflow. |
