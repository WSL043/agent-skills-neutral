# Skill Implementation and Retention Review

All 45 entries are original vendor-neutral syntheses. No provider-only, deprecated, placeholder, or organization-specific skill remains active.

## Conditional items

Twelve B-level skills remain for reference but are excluded from the default profile. Seven are broad platform guides and five are niche or tool-light workflows. `migrate-test-fixtures` is explicitly experimental and is the weakest retained item.

| Level | Skill | Concrete implementation | Risk or limitation | Routing decision |
|---|---|---|---|---|
| A | `discover-agent-skills` | Manifest-first discovery followed by full-body, dependency, trust, overlap, and license inspection before recommendation. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `build-mcp-server` | Model operations as narrow MCP tools/resources with schemas, transport-independent domain logic, structured errors, and client tests. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `design-codebase` | Map dependencies and change pressure, compare structural options, then stage an incremental migration with rollback points. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `model-domain` | Build a glossary, entities/values, invariants, events, state transitions, ownership, and bounded contexts from real scenarios. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `work-with-docx` | Route between high-level DOCX libraries and surgical OOXML editing, then reopen and render for layout verification. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `work-with-pdf` | Route structural extraction/editing, forms, OCR, generation, merge/split, and rendered page QA through one entry. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `work-with-pptx` | Use editable generation for new decks, OOXML/package editing for high-fidelity templates, and rendered slide QA. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `work-with-xlsx` | Use dataframe/workbook libraries normally and package-level XML edits when macros, pivots, or unsupported features must survive. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `build-cli` | Separate parsing, domain logic, I/O, and presentation; define configuration precedence, stdout/stderr, JSON output, and exit-code tests. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `capture-screen` | Capture the smallest screen/window region with known scale and coordinates, then inspect privacy, clipping, and dimensions. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `resolve-merge-conflicts` | Inspect base/ours/theirs and relevant commits, integrate both intents, regenerate derived files, and test both change sets. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `use-git-worktrees` | Create an explicit isolated path and branch, verify baseline state, and clean up only after reachability is confirmed. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `work-with-jupyter-notebook` | Organize a reproducible narrative, move reusable logic to modules, restart the kernel, and execute all cells in order. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `finish-development-branch` | Inspect the branch/diff, run final checks, choose merge/PR/handoff safely, and preserve reachability before cleanup. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `orchestrate-agent-work` | Keep the blocker local, delegate disjoint side tasks with explicit outputs, then integrate and run cross-task checks. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `prototype-solution` | Time-box the smallest visual or executable artifact that can accept/reject one explicit uncertainty. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `coauthor-documents` | Agree audience and outline first, draft claims with evidence section by section, then run a fresh-reader pass. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `research-primary-sources` | Discover broadly, read authoritative primary sources, track versions and contradictions, and cite each material claim. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `map-security-ownership` | Combine sensitive-path mapping, ownership files, history, review activity, recency, and concentration metrics. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `review-security-practices` | Trace real security data flows, compare current official guidance, validate reachable findings, and prioritize hardening with verification. | No material design defect found; still requires task-local tools and verification. | Stable but exact-trigger-only to prevent false-positive security routing. |
| A | `threat-model-system` | Map assets, flows, trust boundaries, attacker goals, attack paths, controls, owners, and residual risk. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `design-frontend` | Set visual direction and tokens, design critical and failure states, implement responsive components, and review accessibility and polish. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `design-motion` | Name motion purpose and exact timing/easing/interruption behavior, prototype in context, and test reduced motion and performance. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| A | `design-visual-theme` | Create semantic color/type/spacing tokens, test representative content, and apply them through shared styles. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| B | `configure-pre-commit` | Reuse pinned repository-native fast checks with file filters; keep slow or networked checks in pre-push/CI. | Low incremental value for experienced agents; keep only for explicit repository hook work. | Explicit trigger only; never in default profile. |
| B | `migrate-test-fixtures` | Inventory fixture consumers and build a deterministic dry-run converter, then compare scenario semantics before removing the source format. | Weakest retained item: upstream inspiration was ShoeHorn-specific; this generic rewrite is an experimental checklist, not a bundled converter. | Experimental and exact-trigger-only; remove if it proves unused. |
| B | `scaffold-exercises` | Define learning objectives, starter state, tests, hints, and a separate reference solution verified from a clean environment. | Niche educational workflow with no bundled starter templates; explicit training tasks only. | Explicit trigger only; never in default profile. |
| B | `build-android-app` | Guide a Kotlin/Compose vertical slice with lifecycle, permissions, accessibility, unit/UI tests, and emulator/device checks. | Broad platform guide with no project template or pinned SDK; project conventions and current docs remain authoritative. | Explicit trigger only; never in default profile. |
| B | `build-flutter-app` | Guide a Flutter vertical slice with explicit state, adaptive UI, target adapters, widget tests, and release builds per target. | Broad platform guide with no project template or pinned SDK; explicit Flutter tasks only. | Explicit trigger only; never in default profile. |
| B | `build-ios-app` | Guide a SwiftUI/UIKit slice with state ownership, concurrency/cancellation, accessibility, tests, and simulator/archive checks. | Broad platform guide with no project template or pinned SDK; requires Apple tooling and project context. | Explicit trigger only; never in default profile. |
| B | `build-react-native-app` | Guide shared React Native behavior with small native adapters, performance controls, tests, and fresh Android/iOS builds. | Broad platform guide; React Native version and native architecture materially affect implementation. | Explicit trigger only; never in default profile. |
| B | `build-winui-app` | Guide WinUI/XAML state, packaging mode, DPI/theme/accessibility, tests, and runtime verification on Windows. | Broad platform guide; Windows SDK, packaging, and deployment model must be inspected live. | Explicit trigger only; never in default profile. |
| B | `create-visual-art` | Define concept and visual system, build a low-fidelity composition, render at target dimensions, and verify print/screen constraints. | No rendering engine is bundled and it overlaps image-generation tools; use as art-direction workflow only. | Explicit trigger only; never in default profile. |
| B | `develop-shaders` | Build a minimal test shader, visualize intermediate spaces/ranges, handle numerical edges, capture references, and profile GPU cost. | Broad and hardware/runtime-sensitive; requires an existing renderer and profiling tools. | Explicit trigger only; never in default profile. |
| B | `build-aspnet-core` | Guide an ASP.NET Core vertical slice with contracts, DI lifetimes, cancellation, auth, problem responses, tests, and production-like startup. | Broad framework guide; target .NET version and repository conventions must be inspected live. | Explicit trigger only; never in default profile. |
| B | `build-fullstack-app` | Guide one thin end-to-end slice across UI, server contract, data, auth, browser tests, and production build behavior. | High overlap with frontend/backend/testing skills; route only for explicit end-to-end full-stack work. | Explicit trigger only; never in default profile. |
| S | `create-agent-skill` | Standard frontmatter, concise trigger description, progressive disclosure, with-skill/baseline evaluation, and structural validation. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| S | `develop-with-tdd` | Run a strict red-green-refactor cycle around observable behavior, with characterization tests for legacy code. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
| S | `diagnose-software` | Reproduce and minimize, instrument the first divergence, test one falsifiable hypothesis, fix the root cause, and add regression evidence. | No material design defect found; still requires task-local tools and verification. | Default core profile. |
| S | `review-code` | Review requirement fit and implementation quality separately, validate reachable findings, and support self/request/receive review modes. | No material design defect found; still requires task-local tools and verification. | Default core profile. |
| S | `verify-completion` | Map each claim to a fresh authoritative check, verify side effects and final state, and narrow claims when checks are blocked. | No material design defect found; still requires task-local tools and verification. | Default core profile. |
| S | `execute-plan` | Validate plan assumptions, execute dependency-ordered batches, run step evidence, and stop when a new decision changes scope. | No material design defect found; still requires task-local tools and verification. | Default core profile. |
| S | `plan-implementation` | Inspect the repository and produce dependency-ordered file/symbol steps with tests, migration, rollback, and acceptance evidence. | No material design defect found; still requires task-local tools and verification. | Default core profile. |
| S | `clarify-requirements` | Separate facts, assumptions, constraints, and open decisions; ask only high-information questions and produce testable acceptance criteria. | No material design defect found; still requires task-local tools and verification. | Default core profile. |
| S | `test-web-app` | Control server readiness and browser lifecycle, use semantic selectors, collect assertions/console/network/screenshots, and rerun fresh. | No material design defect found; still requires task-local tools and verification. | Stable on-demand route. |
